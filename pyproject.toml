[project]
name = "hass-lisios-bridge"
version = "0.1.0"
requires-python = ">=3.13.2"
dependencies = [
    "homeassistant>=2025.6.3",
]

[dependency-groups]
dev = [
    "colorlog>=6.9.0",
    "ruff>=0.12.0",
]
